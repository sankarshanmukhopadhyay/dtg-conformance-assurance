# Risk → Control Objective mapping

This folder contains CSV mappings that connect identified risks to the control objectives that mitigate them.

- Canonical risk export: `risk/exports/risk_assessment.csv`
- Control objective registry: `controls/control_objectives.csv`
- Mapping table: `risk_to_control_objectives.csv`

Run `make validate` to check basic structural integrity and references.
