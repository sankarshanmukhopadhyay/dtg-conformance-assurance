# Templates

CSV-first templates used to produce interoperable DCAS evidence bundles.

- Start with: `templates/starter-bundle/`

The required columns and validation rules are defined in machine-readable form under `schemas/csv/`.
