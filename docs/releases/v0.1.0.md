# v0.1.0 — Adopter-grade DCAS baseline

This is the first tagged release of **DTG Conformance & Assurance (DCAS)**.

## What this release delivers

### 1) A clear adoption front door
- Role-based onboarding (`the referenced artifact (not included in this repo)`, `docs/roles/`)
- Repository map and FAQ (`the referenced artifact (not included in this repo)`, `the referenced artifact (not included in this repo)`)
- Updated README routing for first-time adopters

### 2) A repeatable evaluation method (“DCAS Evaluate”)
- Versioned method package (`DCAS_METHOD_VERSION`, `docs/dcas-evaluate/`)
- Report template, scoring + confidence rubric, and evidence checklist template

### 3) Minimum viable traceability spine (machine-readable)
- Populated catalogs:
  - controls: `the referenced artifact (not included in this repo)`
  - evidence: `the referenced artifact (not included in this repo)`
  - tests: `the referenced artifact (not included in this repo)`
- Risk → CO mapping: `the referenced artifact (not included in this repo)`
- Generated coverage snapshot: `the referenced artifact (not included in this repo)`

### 4) Cleaner exports and tooling
- Regenerated and normalized `the referenced artifact (not included in this repo)`
- Updated export/validation scripts in `tools/`
- Repo hygiene improvements (`.gitignore`, removed stray files)

## Compatibility and expectations

- DCAS remains **implementation-neutral**: no protocol, DID method, ledger, or product is mandated.
- Catalogs and IDs are stable within the release; avoid renumbering without a release.

## Next

See `the referenced artifact (not included in this repo)` for the next increment (automation + worked examples + deeper control granularity).
