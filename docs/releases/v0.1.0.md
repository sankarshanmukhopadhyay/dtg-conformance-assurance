# v0.1.0 — Adopter-grade DCAS baseline

This is the first tagged release of **DTG Conformance & Assurance (DCAS)**.

## What this release delivers

### 1) A clear adoption front door
- Role-based onboarding (`an implementer-supplied artifact appropriate to your context (e.g., risk register entry, test evidence, or approval record).`, `docs/roles/`)
- Repository map and FAQ (`an implementer-supplied artifact appropriate to your context (e.g., risk register entry, test evidence, or approval record).`, `an implementer-supplied artifact appropriate to your context (e.g., risk register entry, test evidence, or approval record).`)
- Updated README routing for first-time adopters

### 2) A repeatable evaluation method (“DCAS Evaluate”)
- Versioned method package (`DCAS_METHOD_VERSION`, `docs/dcas-evaluate/`)
- Report template, scoring + confidence rubric, and evidence checklist template

### 3) Minimum viable traceability spine (machine-readable)
- Populated catalogs:
  - controls: `an implementer-supplied artifact appropriate to your context (e.g., risk register entry, test evidence, or approval record).`
  - evidence: `an implementer-supplied evidence artifact (e.g., audit report excerpt, test output, or signed attestation).`
  - tests: `an implementer-supplied artifact appropriate to your context (e.g., risk register entry, test evidence, or approval record).`
- Risk → CO mapping: `an implementer-supplied artifact appropriate to your context (e.g., risk register entry, test evidence, or approval record).`
- Generated coverage snapshot: `an implementer-supplied artifact appropriate to your context (e.g., risk register entry, test evidence, or approval record).`

### 4) Cleaner exports and tooling
- Regenerated and normalized `an implementer-supplied artifact appropriate to your context (e.g., risk register entry, test evidence, or approval record).`
- Updated export/validation scripts in `tools/`
- Repo hygiene improvements (`.gitignore`, removed stray files)

## Compatibility and expectations

- DCAS remains **implementation-neutral**: no protocol, DID method, ledger, or product is mandated.
- Catalogs and IDs are stable within the release; avoid renumbering without a release.

## Next

See `an implementer-supplied artifact appropriate to your context (e.g., risk register entry, test evidence, or approval record).` for the next increment (automation + worked examples + deeper control granularity).
